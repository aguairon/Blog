<header class="period_header">
	<h2>Periodos historicos</h2>
</header>

<%= link_to 'New Period', new_period_path %>

<% @periods.each do |period| %>
		<div><%= link_to period.title, period_path(period) %></div>
<% end %>