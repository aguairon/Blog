<header class="article-header">
	<h1>Articulos sobre <%= @period.title %>
		<% if admin_signed_in? %>
			<a class="period-header-link"><%= link_to 'New article', new_article_path(period_id: @period) %></a>
		<% end %>
	</h1>
</header>
<div id="articles_list">
	<%= render :partial => "aplication/article", :collection => @articles %>
</div>
