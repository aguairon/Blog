<%= form_with(model: @article) do |form| %>
 
  <% if @article.errors.any? %>
  <div id="error_explanation">
    <h2>
    <%= pluralize(@article.errors.count, "error") %> prohibited this article from being saved:
    </h2>
    <ul>
    <% @article.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>

  <%= form.hidden_field :period_id, :value => @article.period_id%>

  <div><%=form.label :title, 'Título' %>
  <%= form.text_field :title, required: true, autofocus: true, minlength: 5%>
  <%=form.label :text, 'Cuerpo del artículo' %>
  <%= form.text_area :text, required: true, autofocus: true, minlength: 5 %>
  <%= form.submit 'Guardar artículo' %>
 
<% end %>